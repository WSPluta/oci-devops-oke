version: 0.1             
component: build
timeoutInSeconds: 1000
failImmediatelyOnError: true
shell: bash           

steps:
  - type: Command
    name: "Install dependencies"
    command: |
      echo $PATH
      cd src/hello-server